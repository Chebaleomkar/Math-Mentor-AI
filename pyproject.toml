[project]
name = "math-mentor-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain==0.1.20",
    "langchain-core==0.1.52",
]
